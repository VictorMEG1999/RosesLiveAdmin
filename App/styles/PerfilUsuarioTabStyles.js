import { StyleSheet, StatusBar } from 'react-native';

const styles = StyleSheet.create({
     containerBuscador: {
        paddingHorizontal: 5,
        height: 44,
        width: '100%',
        marginVertical: 10,
      },
      menu: {
        borderWidth: 1,
        borderColor: '#E01983',
        height: '150%',
        flex: 1,
      },
      menuText: {
        fontSize: 20,
        fontWeight: 'bold',
        color: 'black',
        textAlign: 'right',
         height: '150%',
      },
});

export default styles;